export const enum View {
    list = 1,
    tile,
}

export const enum PageSize {
    four = 4,
    five = 5,
    ten = 10,
    max = 20,
}

export const enum SortPerPage {
    none = 0,
    byRank,
    byTitle,
    byRelease,
}
